_gitlab_version_underscored: "{{ gitlab_version | regex_replace('\\.', '_') }}"

gitlab_docker_prod_hostname: >-
    gitlab-docker_gitlab_v{{ _gitlab_version_underscored }}_1

docker_network_name: "gitlab-docker_default"  # For historical reasons
